Bu klasör, proje şartnamesinde istenen üst düzey `public/` klasörüdür.

Next.js uygulaması kendi statik dosyalarını `frontend/public/` altında barındırır
(favicon, robots.txt vb.). Bu kök klasör; ileride backend tarafından servis edilecek
genel/paylaşılan statik varlıklar (ör. e-posta şablonlarında kullanılan logo) için
ayrılmıştır.
